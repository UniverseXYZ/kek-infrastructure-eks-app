apiVersion: apps/v1
kind: Deployment
metadata:
  name: scrape
spec:
  template:
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: component
                  operator: In
                  values:
                  - scrape
              topologyKey: topology.kubernetes.io/zone
