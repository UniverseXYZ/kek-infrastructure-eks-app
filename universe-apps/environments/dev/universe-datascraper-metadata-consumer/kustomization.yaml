apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/universe-datascraper-metadata-consumer
  - secret.yaml
patchesStrategicMerge:
  - patch-affinity.yaml
patchesJson6902:
  - path: patch-replicas.yaml
    target:
      group: apps
      kind: Deployment
      name: universe-datascraper-metadata-consumer
      version: v1
commonLabels:
  app: indexer
  component: universe-datascraper-metadata-consumer
  environment: dev
configMapGenerator:
  - name: universe-datascraper-metadata-consumer
    literals:
      - AWS_REGION=us-east-1
      - AWS_ACCESS_KEY_ID=AKIARDONMETSALD6H4TH
      - AWS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/076129510628/datascraper-token-dlq.fifo
      - ETHEREUM_NETWORK=rinkeby
      - INFURA_PROJECT_ID=978f8f87fbdd4ea0b94fe0ab245b06db
      - SESSION_SECRET=XYZDAO
      - IPFS_GATEWAY=https://cloudflare-ipfs.com/ipfs/
      - SPECIAL_CASES_OPEN_SEA_1155_URI=https://api.opensea.io/api/v1/metadata
      - APP_ENV=dev
      - PORT=8080